# Persons Database setup
version: '3'
services:
  persons-mysql-server:
    image: mysql:latest
    ports:
      - 3306:3306
    environment:
      - "MYSQL_ROOT_PASSWORD=root"
    volumes:
      - ./mysql/init:/docker-entrypoint-initdb.d
    networks:
    - persons-net

networks:
  persons-net: